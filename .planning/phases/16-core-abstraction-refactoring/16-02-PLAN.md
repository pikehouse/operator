---
phase: 16-core-abstraction-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - packages/operator-tikv/pyproject.toml
  - packages/operator-tikv/src/operator_tikv/invariants.py
  - packages/operator-tikv/src/operator_tikv/subject.py
  - packages/operator-tikv/src/operator_tikv/factory.py
  - packages/operator-tikv/src/operator_tikv/__init__.py
autonomous: true

must_haves:
  truths:
    - "TiKVSubject implements SubjectProtocol with observe() method"
    - "TiKVInvariantChecker implements InvariantCheckerProtocol with check() method"
    - "InvariantViolation is imported from operator_protocols, not defined locally"
    - "Factory function creates subject and checker together"
    - "All existing operator-tikv tests pass"
  artifacts:
    - path: "packages/operator-tikv/src/operator_tikv/subject.py"
      provides: "TiKVSubject with observe() returning dict[str, Any]"
      contains: "async def observe"
      min_lines: 50
    - path: "packages/operator-tikv/src/operator_tikv/invariants.py"
      provides: "TiKVInvariantChecker with check(observation) method"
      contains: "def check(self, observation"
      min_lines: 100
    - path: "packages/operator-tikv/src/operator_tikv/factory.py"
      provides: "create_tikv_subject_and_checker factory function"
      contains: "def create_tikv_subject_and_checker"
      min_lines: 15
  key_links:
    - from: "packages/operator-tikv/src/operator_tikv/invariants.py"
      to: "operator_protocols.invariant"
      via: "import InvariantViolation"
      pattern: "from operator_protocols(\\.invariant)? import.*InvariantViolation"
    - from: "packages/operator-tikv/src/operator_tikv/subject.py"
      to: "operator_protocols.types"
      via: "import generic types"
      pattern: "from operator_protocols(\\.types)? import.*(Store|StoreMetrics)"
---

<objective>
Update operator-tikv to implement the new generic protocols from operator-protocols.

Purpose: Make TiKVSubject and TiKVInvariantChecker implement the generic protocols while maintaining backward compatibility. Add factory function for CLI integration.

Output: Updated operator-tikv package that uses operator-protocols types and implements the new protocols.
</objective>

<execution_context>
@/Users/jrtipton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jrtipton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-core-abstraction-refactoring/16-CONTEXT.md
@.planning/phases/16-core-abstraction-refactoring/16-RESEARCH.md
@.planning/phases/16-core-abstraction-refactoring/16-01-SUMMARY.md

# Current TiKV implementation to update:
@packages/operator-tikv/src/operator_tikv/subject.py
@packages/operator-tikv/src/operator_tikv/invariants.py
@packages/operator-tikv/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator-tikv dependencies and imports</name>
  <files>packages/operator-tikv/pyproject.toml, packages/operator-tikv/src/operator_tikv/__init__.py</files>
  <action>
Update package dependencies and re-exports:

1. Update `pyproject.toml`:
   - Add `operator-protocols` as a dependency
   - Keep existing dependencies (httpx, pydantic, operator-core)

2. Update `__init__.py`:
   - Remove local InvariantViolation export (it now comes from operator_protocols)
   - Keep TiKVSubject, InvariantChecker (now TiKVInvariantChecker) exports
   - Add create_tikv_subject_and_checker to exports

Ensure clean import structure that doesn't cause circular dependencies.
  </action>
  <verify>
`cd /Users/jrtipton/x/operator && uv pip install -e packages/operator-protocols -e packages/operator-tikv --quiet && python -c "from operator_tikv import TiKVSubject; print('OK')"`
  </verify>
  <done>operator-tikv depends on operator-protocols and imports work</done>
</task>

<task type="auto">
  <name>Task 2: Update TiKVSubject to implement SubjectProtocol</name>
  <files>packages/operator-tikv/src/operator_tikv/subject.py</files>
  <action>
Update TiKVSubject to implement the generic SubjectProtocol:

1. Add `observe()` method that returns `dict[str, Any]`:
   - Gather stores via `self.pd.get_stores()`
   - Gather cluster metrics
   - Gather store metrics for each up store
   - Return structured dict per RESEARCH.md example:
     ```python
     return {
         "stores": [{"id": s.id, "address": s.address, "state": s.state} for s in stores],
         "cluster_metrics": {...},
         "store_metrics": {...},
     }
     ```

2. Keep existing methods (get_stores, get_store_metrics, etc.) for backward compatibility
   - These are TiKV-specific convenience methods
   - The generic `observe()` calls these internally

3. Import generic types from operator_protocols.types:
   - `from operator_protocols.types import Store, StoreMetrics, ClusterMetrics`
   - Remove imports from operator_core.types (will be deprecated)

4. Keep `get_action_definitions()` method unchanged (already protocol-compliant)

The class does NOT need to explicitly inherit from SubjectProtocol - structural subtyping via Protocol means having the right methods is sufficient.
  </action>
  <verify>
`python -c "from operator_tikv.subject import TiKVSubject; from operator_protocols import SubjectProtocol; print('Protocol check:', hasattr(TiKVSubject, 'observe'), hasattr(TiKVSubject, 'get_action_definitions'))"`
  </verify>
  <done>TiKVSubject has observe() method returning dict and is protocol-compliant</done>
</task>

<task type="auto">
  <name>Task 3: Update InvariantChecker to implement InvariantCheckerProtocol</name>
  <files>packages/operator-tikv/src/operator_tikv/invariants.py, packages/operator-tikv/src/operator_tikv/factory.py</files>
  <action>
Update invariant checker and create factory:

1. Update `invariants.py`:
   - Import `InvariantViolation` from `operator_protocols.invariant` instead of defining locally
   - REMOVE the local `InvariantViolation` dataclass definition
   - Keep `InvariantConfig` dataclass (TiKV-specific configuration)
   - Rename class to `TiKVInvariantChecker` (optional but clearer)
   - Add `check(self, observation: dict[str, Any]) -> list[InvariantViolation]` method:
     - Parse observation dict to extract stores, metrics
     - Call existing check methods (check_stores_up, check_latency, check_disk_space)
     - Return aggregated violations
   - Keep existing check_stores_up, check_latency, check_disk_space methods

2. Create `factory.py` with factory function per RESEARCH.md:
   ```python
   async def create_tikv_subject_and_checker(
       pd_endpoint: str,
       prometheus_url: str,
       pd_http: httpx.AsyncClient | None = None,
       prom_http: httpx.AsyncClient | None = None,
   ) -> tuple[TiKVSubject, TiKVInvariantChecker]:
   ```
   - Creates httpx clients if not provided
   - Returns tuple of (subject, checker)
   - Used by CLI to create subject/checker pair

Import types from operator_protocols, not operator_core.
  </action>
  <verify>
`python -c "from operator_tikv.invariants import TiKVInvariantChecker; from operator_protocols import InvariantCheckerProtocol, InvariantViolation; print('InvariantViolation from protocols:', InvariantViolation.__module__)"`
  </verify>
  <done>TiKVInvariantChecker implements check() method, InvariantViolation comes from protocols, factory exists</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv pip install -e packages/operator-protocols -e packages/operator-tikv` succeeds
- [ ] `python -c "from operator_tikv import TiKVSubject"` works
- [ ] `python -c "from operator_tikv.invariants import TiKVInvariantChecker"` works
- [ ] `python -c "from operator_tikv.factory import create_tikv_subject_and_checker"` works
- [ ] TiKVSubject has `observe()` method returning dict
- [ ] TiKVInvariantChecker has `check(observation)` method
- [ ] InvariantViolation is imported from operator_protocols, not defined in operator_tikv
- [ ] `pytest packages/operator-tikv/tests/ -v` passes (if tests exist)
</verification>

<success_criteria>
- All tasks completed
- TiKVSubject implements SubjectProtocol
- TiKVInvariantChecker implements InvariantCheckerProtocol
- Factory function creates subject and checker
- No InvariantViolation definition in operator_tikv (uses operator_protocols)
</success_criteria>

<output>
After completion, create `.planning/phases/16-core-abstraction-refactoring/16-02-SUMMARY.md`
</output>
