---
phase: 16-core-abstraction-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/operator-protocols/pyproject.toml
  - packages/operator-protocols/src/operator_protocols/__init__.py
  - packages/operator-protocols/src/operator_protocols/subject.py
  - packages/operator-protocols/src/operator_protocols/invariant.py
  - packages/operator-protocols/src/operator_protocols/types.py
autonomous: true

must_haves:
  truths:
    - "operator-protocols package can be imported without errors"
    - "SubjectProtocol is runtime_checkable and defines observe() and get_action_definitions()"
    - "InvariantCheckerProtocol is runtime_checkable and defines check(observation) returning violations"
    - "InvariantViolation dataclass exists with generic entity_id field"
    - "Generic types (Store, StoreMetrics, ClusterMetrics) are importable from operator_protocols"
  artifacts:
    - path: "packages/operator-protocols/pyproject.toml"
      provides: "Package configuration with no external dependencies"
      contains: "name = \"operator-protocols\""
    - path: "packages/operator-protocols/src/operator_protocols/subject.py"
      provides: "SubjectProtocol with observe() and get_action_definitions()"
      contains: "@runtime_checkable"
      min_lines: 20
    - path: "packages/operator-protocols/src/operator_protocols/invariant.py"
      provides: "InvariantCheckerProtocol and InvariantViolation dataclass"
      contains: "class InvariantViolation"
      min_lines: 25
    - path: "packages/operator-protocols/src/operator_protocols/types.py"
      provides: "Generic Store, StoreMetrics, ClusterMetrics types"
      contains: "class Store"
      min_lines: 30
  key_links:
    - from: "packages/operator-protocols/src/operator_protocols/__init__.py"
      to: "subject.py, invariant.py, types.py"
      via: "re-exports"
      pattern: "from operator_protocols\\.(subject|invariant|types) import"
---

<objective>
Create the operator-protocols package with generic Protocol definitions.

Purpose: Establish the base package that defines Subject and InvariantChecker protocols without any TiKV-specific types. This package has zero dependencies on other operator-* packages and serves as the foundation for decoupling.

Output: New `packages/operator-protocols/` package with SubjectProtocol, InvariantCheckerProtocol, InvariantViolation, and generic types.
</objective>

<execution_context>
@/Users/jrtipton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jrtipton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-core-abstraction-refactoring/16-CONTEXT.md
@.planning/phases/16-core-abstraction-refactoring/16-RESEARCH.md

# Current types to extract and generalize:
@packages/operator-core/src/operator_core/types.py
@packages/operator-tikv/src/operator_tikv/invariants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create operator-protocols package structure</name>
  <files>packages/operator-protocols/pyproject.toml, packages/operator-protocols/src/operator_protocols/__init__.py</files>
  <action>
Create the operator-protocols package directory structure:

1. Create `packages/operator-protocols/pyproject.toml`:
   - Use hatchling build system (same as other packages)
   - Package name: "operator-protocols"
   - Python requires: ">=3.11"
   - NO dependencies on other operator-* packages
   - Only dependency: typing-extensions (if needed for Protocol backports, likely not needed for 3.11+)

2. Create `packages/operator-protocols/src/operator_protocols/__init__.py`:
   - Re-export all public types from submodules
   - Include: SubjectProtocol, InvariantCheckerProtocol, InvariantViolation
   - Include: Store, StoreMetrics, ClusterMetrics, StoreId

Follow the exact pyproject.toml pattern from operator-core/pyproject.toml for consistency.
  </action>
  <verify>
`cd /Users/jrtipton/x/operator && uv pip install -e packages/operator-protocols --quiet && python -c "import operator_protocols; print('OK')"`
  </verify>
  <done>Package structure exists and is pip-installable</done>
</task>

<task type="auto">
  <name>Task 2: Define generic Subject and InvariantChecker protocols</name>
  <files>packages/operator-protocols/src/operator_protocols/subject.py, packages/operator-protocols/src/operator_protocols/invariant.py</files>
  <action>
Create protocol definitions per RESEARCH.md and CONTEXT.md decisions:

1. Create `subject.py` with SubjectProtocol:
   - Use `@runtime_checkable` decorator
   - `async def observe(self) -> dict[str, Any]` - returns flexible observation dict
   - `def get_action_definitions(self) -> list["ActionDefinition"]` - returns available actions
   - Include docstrings with example observation schema
   - Use TYPE_CHECKING guard for ActionDefinition import to avoid circular dependencies

2. Create `invariant.py` with:
   - `InvariantViolation` dataclass (moved from operator_tikv.invariants):
     - invariant_name: str
     - message: str
     - first_seen: datetime
     - last_seen: datetime
     - store_id: str | None = None (keep as store_id for backward compatibility per RESEARCH.md)
     - severity: str = "warning"
   - `InvariantCheckerProtocol`:
     - Use `@runtime_checkable` decorator
     - `def check(self, observation: dict[str, Any]) -> list[InvariantViolation]`

Per CONTEXT.md: Observation type is `dict[str, Any]` for maximum flexibility.
  </action>
  <verify>
`python -c "from operator_protocols import SubjectProtocol, InvariantCheckerProtocol, InvariantViolation; print('Protocols defined:', SubjectProtocol, InvariantCheckerProtocol)"`
  </verify>
  <done>SubjectProtocol and InvariantCheckerProtocol are importable and runtime_checkable</done>
</task>

<task type="auto">
  <name>Task 3: Move generic types to operator-protocols</name>
  <files>packages/operator-protocols/src/operator_protocols/types.py</files>
  <action>
Create types.py with generic types extracted from operator_core.types:

1. Copy and adapt from operator_core/types.py:
   - `StoreId = str` type alias (generic - any node identifier)
   - `Store` dataclass (generic - id, address, state works for any distributed system)
   - `StoreMetrics` dataclass (generic - qps, latency, disk, cpu are universal)
   - `ClusterMetrics` dataclass (generic - counts and distributions are universal)

2. DO NOT include:
   - `Region` - TiKV-specific (will stay in operator-tikv)
   - `RegionId` - TiKV-specific (will stay in operator-tikv)

3. Update docstrings to be generic (remove TiKV-specific references):
   - "Store" -> "node in a distributed system"
   - "TiKV store" -> "distributed system node"

Keep the dataclass structure identical to ensure existing code compatibility.
  </action>
  <verify>
`python -c "from operator_protocols import Store, StoreMetrics, ClusterMetrics, StoreId; s = Store(id='1', address='host:1234', state='Up'); print('Store:', s)"`
  </verify>
  <done>Generic types are importable and usable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv pip install -e packages/operator-protocols` succeeds
- [ ] `python -c "from operator_protocols import SubjectProtocol, InvariantCheckerProtocol"` works
- [ ] `python -c "from operator_protocols import InvariantViolation, Store, StoreMetrics, ClusterMetrics"` works
- [ ] Protocol classes have `@runtime_checkable` decorator
- [ ] No imports from operator_core or operator_tikv in operator_protocols
</verification>

<success_criteria>
- All tasks completed
- operator-protocols package installable
- All protocols and types importable
- Zero dependencies on other operator-* packages
</success_criteria>

<output>
After completion, create `.planning/phases/16-core-abstraction-refactoring/16-01-SUMMARY.md`
</output>
