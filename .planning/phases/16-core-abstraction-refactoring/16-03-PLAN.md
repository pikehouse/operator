---
phase: 16-core-abstraction-refactoring
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - packages/operator-core/pyproject.toml
  - packages/operator-core/src/operator_core/monitor/loop.py
  - packages/operator-core/src/operator_core/monitor/types.py
  - packages/operator-core/src/operator_core/db/tickets.py
  - packages/operator-core/src/operator_core/agent/runner.py
  - packages/operator-core/src/operator_core/agent/context.py
  - packages/operator-core/src/operator_core/demo/chaos.py
  - packages/operator-core/src/operator_core/types.py
autonomous: true

must_haves:
  truths:
    - "MonitorLoop accepts any SubjectProtocol and InvariantCheckerProtocol"
    - "operator-core has zero imports from operator_tikv"
    - "MonitorLoop uses subject.observe() and checker.check() generic methods"
    - "TicketDB uses InvariantViolation from operator_protocols"
    - "operator_core.types keeps Region and RegionId for backward compatibility"
  artifacts:
    - path: "packages/operator-core/src/operator_core/monitor/loop.py"
      provides: "Generic MonitorLoop using protocols"
      contains: "from operator_protocols"
      min_lines: 100
    - path: "packages/operator-core/src/operator_core/db/tickets.py"
      provides: "TicketDB using generic InvariantViolation"
      contains: "from operator_protocols"
      min_lines: 50
    - path: "packages/operator-core/src/operator_core/types.py"
      provides: "Re-exports from protocols plus TiKV-specific types"
      contains: "from operator_protocols"
      min_lines: 20
  key_links:
    - from: "packages/operator-core/src/operator_core/monitor/loop.py"
      to: "operator_protocols"
      via: "import protocols"
      pattern: "from operator_protocols import.*(SubjectProtocol|InvariantCheckerProtocol)"
    - from: "packages/operator-core/pyproject.toml"
      to: "operator-protocols"
      via: "dependency"
      pattern: "operator-protocols"
---

<objective>
Remove all TiKV-specific imports from operator-core and use generic protocols.

Purpose: Make operator-core completely subject-agnostic by using protocols instead of concrete TiKV types. This is the critical decoupling step.

Output: operator-core with zero imports from operator_tikv, using SubjectProtocol and InvariantCheckerProtocol throughout.
</objective>

<execution_context>
@/Users/jrtipton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jrtipton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-core-abstraction-refactoring/16-CONTEXT.md
@.planning/phases/16-core-abstraction-refactoring/16-RESEARCH.md
@.planning/phases/16-core-abstraction-refactoring/16-02-SUMMARY.md

# Files to update (from RESEARCH.md coupling audit):
@packages/operator-core/src/operator_core/monitor/loop.py
@packages/operator-core/src/operator_core/monitor/types.py
@packages/operator-core/src/operator_core/db/tickets.py
@packages/operator-core/src/operator_core/agent/runner.py
@packages/operator-core/src/operator_core/agent/context.py
@packages/operator-core/src/operator_core/demo/chaos.py
@packages/operator-core/src/operator_core/types.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update operator-core dependencies and types</name>
  <files>packages/operator-core/pyproject.toml, packages/operator-core/src/operator_core/types.py, packages/operator-core/src/operator_core/__init__.py</files>
  <action>
Update package dependencies and type re-exports:

1. Update `pyproject.toml`:
   - Add `operator-protocols` as a dependency
   - REMOVE `operator-tikv` from dependencies (operator-core should not depend on tikv)

2. Update `types.py`:
   - Re-export generic types from operator_protocols: Store, StoreMetrics, ClusterMetrics, StoreId
   - KEEP Region and RegionId locally (TiKV-specific, for backward compatibility)
   - Add deprecation comment that Region/RegionId should be imported from operator_tikv

3. Update `__init__.py`:
   - Ensure InvariantViolation is re-exported from operator_protocols
   - Keep backward-compatible exports where possible

Goal: External code importing from operator_core.types should still work, but internally operator-core uses protocols.
  </action>
  <verify>
`python -c "from operator_core.types import Store, StoreMetrics, ClusterMetrics, Region; print('Types importable')"`
  </verify>
  <done>operator-core depends on operator-protocols, types re-exported correctly</done>
</task>

<task type="auto">
  <name>Task 2: Update MonitorLoop to use generic protocols</name>
  <files>packages/operator-core/src/operator_core/monitor/loop.py, packages/operator-core/src/operator_core/monitor/types.py</files>
  <action>
Refactor MonitorLoop to be subject-agnostic:

1. Update `monitor/loop.py`:
   - REMOVE: `from operator_tikv.invariants import InvariantChecker, InvariantViolation`
   - REMOVE: `from operator_tikv.subject import TiKVSubject`
   - ADD: `from operator_protocols import SubjectProtocol, InvariantCheckerProtocol, InvariantViolation`
   - Change type annotations:
     - `subject: TiKVSubject` -> `subject: SubjectProtocol`
     - `checker: InvariantChecker` -> `checker: InvariantCheckerProtocol`
   - Update `_check_cycle()` to use generic observation pattern:
     ```python
     observation = await self.subject.observe()
     violations = self.checker.check(observation)
     ```
   - REMOVE TiKV-specific logic (calling get_stores, get_store_metrics directly)
   - The checker.check() method now handles all invariant checking internally

2. Update `monitor/types.py`:
   - REMOVE: `from operator_tikv.invariants import InvariantViolation`
   - ADD: `from operator_protocols import InvariantViolation`
   - Keep `make_violation_key()` function unchanged

This is the core refactoring - MonitorLoop becomes completely generic.
  </action>
  <verify>
`python -c "from operator_core.monitor.loop import MonitorLoop; import inspect; sig = inspect.signature(MonitorLoop.__init__); print('MonitorLoop params:', list(sig.parameters.keys()))"`
  </verify>
  <done>MonitorLoop uses protocols, no TiKV imports in monitor/</done>
</task>

<task type="auto">
  <name>Task 3: Update remaining operator-core files</name>
  <files>packages/operator-core/src/operator_core/db/tickets.py, packages/operator-core/src/operator_core/agent/runner.py, packages/operator-core/src/operator_core/agent/context.py, packages/operator-core/src/operator_core/demo/chaos.py</files>
  <action>
Update all remaining files with TiKV imports:

1. Update `db/tickets.py`:
   - REMOVE: `from operator_tikv.invariants import InvariantViolation`
   - ADD: `from operator_protocols import InvariantViolation`

2. Update `agent/runner.py`:
   - REMOVE: `from operator_tikv.subject import TiKVSubject`
   - ADD: `from operator_protocols import SubjectProtocol`
   - Change type annotation: `subject: TiKVSubject` -> `subject: SubjectProtocol`

3. Update `agent/context.py`:
   - REMOVE: TYPE_CHECKING import of TiKVSubject
   - ADD: TYPE_CHECKING import of SubjectProtocol from operator_protocols
   - Change type annotation: `subject: "TiKVSubject"` -> `subject: "SubjectProtocol"`

4. Update `demo/chaos.py`:
   - This file has heavy TiKV coupling for demo purposes
   - For now, keep TiKV imports but add TODO comment that this should be made generic
   - OR move TiKV-specific demo logic to operator-tikv package
   - DECISION: Keep in operator-core but import from operator_tikv (demo is TiKV-specific)
   - This is acceptable because demo/ is not core functionality

After these changes, grep for "from operator_tikv" should only find demo/chaos.py.
  </action>
  <verify>
`cd /Users/jrtipton/x/operator/packages/operator-core && grep -r "from operator_tikv" src/operator_core/ --include="*.py" | grep -v "demo/chaos.py" | wc -l` should be 0
  </verify>
  <done>All operator-core files except demo/ use protocols, zero TiKV imports in core paths</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `grep -r "from operator_tikv" packages/operator-core/src/operator_core/ | grep -v demo/` returns empty
- [ ] `python -c "from operator_core.monitor.loop import MonitorLoop"` works
- [ ] `python -c "from operator_core.db.tickets import TicketDB"` works
- [ ] MonitorLoop type annotations use SubjectProtocol, not TiKVSubject
- [ ] InvariantViolation imported from operator_protocols throughout
</verification>

<success_criteria>
- All tasks completed
- operator-core has zero TiKV imports outside demo/
- MonitorLoop uses SubjectProtocol and InvariantCheckerProtocol
- All operator-core imports work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/16-core-abstraction-refactoring/16-03-SUMMARY.md`
</output>
