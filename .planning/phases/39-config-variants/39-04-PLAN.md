---
phase: 39-config-variants
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/operator-core/src/operator_core/db/schema.py
  - packages/operator-core/src/operator_core/monitor/types.py
  - packages/operator-core/src/operator_core/db/tickets.py
  - packages/operator-core/src/operator_core/agent_lab/ticket_ops.py
  - packages/operator-core/src/operator_core/agent_lab/loop.py
  - eval/src/eval/runner/harness.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Trial runner uses variant config for agent execution"
    - "Agent loop reads variant config from ticket record"
    - "Variant config flows from harness through tickets table to process_ticket"
  artifacts:
    - path: "packages/operator-core/src/operator_core/db/schema.py"
      provides: "Variant columns in tickets table"
      contains: "variant_model TEXT"
    - path: "packages/operator-core/src/operator_core/agent_lab/loop.py"
      provides: "process_ticket uses variant config from ticket"
      contains: "ticket.variant_model"
    - path: "eval/src/eval/runner/harness.py"
      provides: "run_trial writes variant config to ticket"
      contains: "variant_model"
  key_links:
    - from: "eval/src/eval/runner/harness.py"
      to: "packages/operator-core/src/operator_core/db/schema.py"
      via: "harness writes variant_model, variant_system_prompt to ticket record"
      pattern: "UPDATE tickets SET variant_model"
    - from: "packages/operator-core/src/operator_core/agent_lab/loop.py"
      to: "packages/operator-core/src/operator_core/db/schema.py"
      via: "process_ticket reads variant config from ticket.variant_model"
      pattern: "ticket\\.variant_model"
---

<objective>
Close the gap where variant_config is loaded but not applied during trial execution.

Purpose: Enable actual A/B testing of agent configurations - model, system_prompt, and tools_config should be used from the variant instead of hardcoded values.

Output: Variant config flows from campaign YAML through harness, into the tickets table (the existing operator.db communication channel), and is read by the agent loop when processing tickets.

Architecture insight: The agent runs as a separate process that polls operator.db for tickets. Environment variables won't work across this process boundary. The solution is to store variant config in the tickets table (similar to how subject_context already works), then have process_ticket read it from the ticket record.
</objective>

<execution_context>
@/Users/jrtipton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jrtipton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Gap being closed
@.planning/phases/39-config-variants/39-VERIFICATION.md

# Prior phase context
@.planning/phases/39-config-variants/39-02-SUMMARY.md

# Files to modify - schema and types
@packages/operator-core/src/operator_core/db/schema.py
@packages/operator-core/src/operator_core/monitor/types.py
@packages/operator-core/src/operator_core/db/tickets.py
@packages/operator-core/src/operator_core/agent_lab/ticket_ops.py

# Files to modify - agent loop
@packages/operator-core/src/operator_core/agent_lab/loop.py

# Files to modify - harness
@eval/src/eval/runner/harness.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add variant columns to tickets table schema</name>
  <files>packages/operator-core/src/operator_core/db/schema.py, packages/operator-core/src/operator_core/monitor/types.py</files>
  <action>
Add variant_model, variant_system_prompt, variant_tools_config columns to the tickets table. This follows the existing pattern of subject_context - data stored in ticket record, read by agent.

1. In schema.py, add three new columns to SCHEMA_SQL after subject_context (line 34):
```python
    subject_context TEXT,                  -- Subject-specific agent prompt context
    variant_model TEXT,                    -- Variant model override (e.g., claude-sonnet-4-20250514)
    variant_system_prompt TEXT,            -- Variant system prompt override
    variant_tools_config TEXT,             -- Variant tools config JSON
    created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,
```

2. In monitor/types.py, add matching fields to Ticket dataclass after subject_context (line 77):
```python
    subject_context: str | None = None
    variant_model: str | None = None
    variant_system_prompt: str | None = None
    variant_tools_config: str | None = None  # JSON string
    created_at: datetime | None = None
```
  </action>
  <verify>
    cd /Users/jrtipton/x/operator && python -c "
from operator_core.db.schema import SCHEMA_SQL
assert 'variant_model TEXT' in SCHEMA_SQL, 'variant_model column missing'
assert 'variant_system_prompt TEXT' in SCHEMA_SQL, 'variant_system_prompt column missing'
print('PASS: Schema has variant columns')
"
  </verify>
  <done>Tickets table schema includes variant_model, variant_system_prompt, variant_tools_config columns</done>
</task>

<task type="auto">
  <name>Task 2: Update ticket DB classes to read/write variant fields</name>
  <files>packages/operator-core/src/operator_core/db/tickets.py, packages/operator-core/src/operator_core/agent_lab/ticket_ops.py</files>
  <action>
Update both TicketDB (async) and TicketOpsDB (sync) to handle the new variant fields.

1. In db/tickets.py, update _row_to_ticket() to read variant fields (around line 100-120):
```python
    variant_model=row["variant_model"],
    variant_system_prompt=row["variant_system_prompt"],
    variant_tools_config=row["variant_tools_config"],
```

2. In agent_lab/ticket_ops.py, update poll_for_open_ticket() to read variant fields (around line 70-85):
   The SELECT already uses `*` so it will include new columns. Update the Ticket construction:
```python
    variant_model=row["variant_model"],
    variant_system_prompt=row["variant_system_prompt"],
    variant_tools_config=row["variant_tools_config"],
```

Note: The create_or_update_ticket() method in tickets.py does NOT need to accept these new fields as parameters. The variant config will be written by the harness via direct SQL UPDATE after ticket creation, not via the monitor loop. This keeps the monitor loop unchanged.
  </action>
  <verify>
    cd /Users/jrtipton/x/operator && python -c "
import sqlite3
import tempfile
from pathlib import Path
from operator_core.db.schema import SCHEMA_SQL, AGENT_SCHEMA_SQL

# Create temp db with schema
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)

conn = sqlite3.connect(db_path)
conn.executescript(SCHEMA_SQL)
conn.executescript(AGENT_SCHEMA_SQL)

# Verify columns exist
cursor = conn.execute('PRAGMA table_info(tickets)')
columns = [row[1] for row in cursor.fetchall()]
assert 'variant_model' in columns, 'variant_model column missing'
assert 'variant_system_prompt' in columns, 'variant_system_prompt column missing'
conn.close()
db_path.unlink()
print('PASS: Ticket columns verified')
"
  </verify>
  <done>TicketDB and TicketOpsDB read variant fields from ticket records</done>
</task>

<task type="auto">
  <name>Task 3: Update process_ticket to use variant config from ticket</name>
  <files>packages/operator-core/src/operator_core/agent_lab/loop.py</files>
  <action>
Modify process_ticket() to read variant config from the ticket record and use it for the API call. This is the key change that makes variant config actually affect agent behavior.

1. Update the model/prompt logic in process_ticket (around line 43-54):
```python
def process_ticket(
    client: anthropic.Anthropic,
    ticket: Ticket,
    audit_db: AuditLogDB,
    session_id: str,
) -> tuple[str, str]:
    """Process ticket with Claude using tool_runner.

    Variant config is read from ticket.variant_model and ticket.variant_system_prompt
    if set by the eval harness. Otherwise uses defaults.
    """
    # Build ticket description
    ticket_text = f"Ticket #{ticket.id}: {ticket.invariant_name}\n{ticket.message}"
    if ticket.metric_snapshot:
        ticket_text += f"\n\nMetrics:\n{json.dumps(ticket.metric_snapshot, indent=2)}"

    # Use variant model if set, otherwise default
    effective_model = ticket.variant_model or "claude-opus-4-20250514"

    # Build system prompt: variant override > default, then append subject context
    base_prompt = ticket.variant_system_prompt or SYSTEM_PROMPT
    effective_system_prompt = base_prompt
    if ticket.subject_context:
        effective_system_prompt += "\n\n" + ticket.subject_context

    # Run Claude with tool_runner
    runner = client.beta.messages.tool_runner(
        model=effective_model,
        max_tokens=8192,
        tools=[shell],
        system=effective_system_prompt,
        messages=[{"role": "user", "content": ticket_text}],
    )
```

Note: The function signature does NOT change. Variant config comes from the ticket record, not function parameters. This preserves backward compatibility with run_agent_loop() which doesn't need modification.
  </action>
  <verify>
    cd /Users/jrtipton/x/operator && python -c "
from operator_core.agent_lab.loop import process_ticket
import inspect
# Verify function exists and signature unchanged (backward compatible)
sig = inspect.signature(process_ticket)
params = list(sig.parameters.keys())
assert params == ['client', 'ticket', 'audit_db', 'session_id'], f'Unexpected params: {params}'
print('PASS: process_ticket signature unchanged (backward compatible)')
"
  </verify>
  <done>process_ticket reads variant_model and variant_system_prompt from ticket record</done>
</task>

<task type="auto">
  <name>Task 4: Harness writes variant config to ticket after creation</name>
  <files>eval/src/eval/runner/harness.py</files>
  <action>
Update run_trial() to write variant config to the ticket record via operator.db. The harness waits for ticket creation, then updates the ticket with variant config before the agent processes it.

1. Add variant_config parameter to run_trial signature (line 124):
```python
async def run_trial(
    subject: EvalSubject,
    chaos_type: str,
    campaign_id: int,
    baseline: bool = False,
    operator_db_path: Path | None = None,
    chaos_params: dict[str, Any] | None = None,
    variant_config: VariantConfig | None = None,
) -> Trial:
```

2. After wait_for_ticket_resolution call (around line 183-185), add function to update ticket with variant:
```python
async def update_ticket_variant(
    operator_db_path: Path,
    variant_config: VariantConfig,
) -> None:
    """Update most recent ticket with variant config."""
    def update():
        conn = sqlite3.connect(operator_db_path)
        conn.execute(
            """
            UPDATE tickets
            SET variant_model = ?, variant_system_prompt = ?, variant_tools_config = ?
            WHERE id = (SELECT MAX(id) FROM tickets)
            """,
            (
                variant_config.model,
                variant_config.system_prompt,
                json.dumps(variant_config.tools_config),
            ),
        )
        conn.commit()
        conn.close()
    await asyncio.to_thread(update)
```

3. In run_trial, after chaos injection but BEFORE waiting for resolution (around line 170), write variant config:
```python
    # Inject chaos
    console.print(f"[bold yellow]Injecting chaos: {chaos_type}[/bold yellow]")
    chaos_injected_at = now()
    chaos_metadata = await subject.inject_chaos(chaos_type, **(chaos_params or {}))

    # Write variant config to ticket (if variant and operator_db provided)
    # Do this early so the agent picks it up when it polls
    if variant_config and operator_db_path and not baseline:
        # Wait briefly for ticket to be created by monitor
        await asyncio.sleep(2.0)
        await update_ticket_variant(operator_db_path, variant_config)
        console.print(f"[dim]Variant config written to ticket: {variant_config.model}[/dim]")
```

4. In run_campaign_from_config(), pass variant_config to run_trial (around line 340):
```python
trial = await run_trial(
    subject=subject,
    chaos_type=spec["chaos_type"] if not spec["baseline"] else "none",
    campaign_id=campaign_id,
    baseline=spec["baseline"],
    operator_db_path=operator_db_path,
    chaos_params=spec["chaos_params"],
    variant_config=variant_config,
)
```
  </action>
  <verify>
    cd /Users/jrtipton/x/operator && python -c "
from eval.runner.harness import run_trial
import inspect
sig = inspect.signature(run_trial)
params = list(sig.parameters.keys())
assert 'variant_config' in params, 'variant_config not in run_trial'
print('PASS: run_trial accepts variant_config parameter')
"
  </verify>
  <done>Harness writes variant config to ticket record via operator.db UPDATE</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Verify schema has variant columns:
```bash
cd /Users/jrtipton/x/operator && python -c "
from operator_core.db.schema import SCHEMA_SQL
assert 'variant_model TEXT' in SCHEMA_SQL
assert 'variant_system_prompt TEXT' in SCHEMA_SQL
assert 'variant_tools_config TEXT' in SCHEMA_SQL
print('PASS: Schema has variant columns')
"
```

2. Verify Ticket dataclass has variant fields:
```bash
cd /Users/jrtipton/x/operator && python -c "
from operator_core.monitor.types import Ticket
import dataclasses
fields = {f.name for f in dataclasses.fields(Ticket)}
assert 'variant_model' in fields
assert 'variant_system_prompt' in fields
assert 'variant_tools_config' in fields
print('PASS: Ticket has variant fields')
"
```

3. Verify run_trial accepts variant_config:
```bash
cd /Users/jrtipton/x/operator && python -c "
from eval.runner.harness import run_trial
import inspect
sig = inspect.signature(run_trial)
params = list(sig.parameters.keys())
assert 'variant_config' in params, 'variant_config not in run_trial'
print('PASS: run_trial has variant_config parameter')
"
```

4. Integration test - variant config flows through database:
```bash
cd /Users/jrtipton/x/operator && python -c "
import sqlite3
import tempfile
import json
from pathlib import Path
from datetime import datetime
from operator_core.db.schema import SCHEMA_SQL, AGENT_SCHEMA_SQL
from operator_core.agent_lab.ticket_ops import TicketOpsDB

# Create temp db with schema
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db_path = Path(f.name)

conn = sqlite3.connect(db_path)
conn.executescript(SCHEMA_SQL)
conn.executescript(AGENT_SCHEMA_SQL)

# Insert ticket with variant config
now = datetime.now().isoformat()
conn.execute('''
    INSERT INTO tickets (violation_key, invariant_name, message, severity, first_seen_at, last_seen_at, variant_model, variant_system_prompt)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?)
''', ('test:1', 'test_invariant', 'Test message', 'warning', now, now, 'claude-sonnet-4-20250514', 'Custom prompt'))
conn.commit()
conn.close()

# Read back with TicketOpsDB
with TicketOpsDB(db_path) as db:
    ticket = db.poll_for_open_ticket()
    assert ticket is not None
    assert ticket.variant_model == 'claude-sonnet-4-20250514'
    assert ticket.variant_system_prompt == 'Custom prompt'

db_path.unlink()
print('PASS: Variant config flows through database')
"
```
</verification>

<success_criteria>
- Schema includes variant_model, variant_system_prompt, variant_tools_config columns
- Ticket dataclass has matching variant fields
- run_trial() signature includes variant_config: VariantConfig | None parameter
- Harness writes variant config to ticket record via SQL UPDATE
- TicketOpsDB reads variant fields when polling for tickets
- process_ticket() uses ticket.variant_model and ticket.variant_system_prompt
- Gap closed: variant config flows from harness -> tickets table -> agent via existing operator.db polling mechanism
</success_criteria>

<output>
After completion, create `.planning/phases/39-config-variants/39-04-SUMMARY.md`
</output>
