---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - packages/operator-core/pyproject.toml
  - packages/operator-core/src/operator_core/__init__.py
autonomous: true

must_haves:
  truths:
    - "uv sync installs all dependencies without errors"
    - "Package imports work from project root"
    - "Workspace structure follows uv conventions"
  artifacts:
    - path: "pyproject.toml"
      provides: "Workspace root configuration"
      contains: "tool.uv.workspace"
    - path: "packages/operator-core/pyproject.toml"
      provides: "Core package configuration"
      contains: "typer"
    - path: "packages/operator-core/src/operator_core/__init__.py"
      provides: "Package entry point"
  key_links:
    - from: "pyproject.toml"
      to: "packages/operator-core"
      via: "workspace members"
      pattern: 'members.*packages'
---

<objective>
Set up the uv workspace monorepo structure with the operator-core package.

Purpose: Establish the project foundation that all subsequent plans build upon. The workspace structure enables clean package separation between core abstractions and subject implementations.

Output: Working uv workspace with operator-core package containing all required dependencies (typer, rich, python-on-whales, httpx, pydantic).
</objective>

<execution_context>
@/Users/jrtipton/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jrtipton/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create uv workspace root</name>
  <files>pyproject.toml</files>
  <action>
Create the workspace root pyproject.toml with:
- Project name: "operator"
- Version: "0.1.0"
- requires-python: ">=3.11"
- Build system: hatchling
- tool.uv.workspace.members = ["packages/*"]

Do NOT add dependencies at the root level - dependencies go in individual packages.

Reference the uv workspace config pattern from RESEARCH.md.
  </action>
  <verify>
Run `uv sync` - should complete without errors (may warn about no dependencies, that's fine)
  </verify>
  <done>
pyproject.toml exists at project root with valid workspace configuration
  </done>
</task>

<task type="auto">
  <name>Task 2: Create operator-core package</name>
  <files>
packages/operator-core/pyproject.toml
packages/operator-core/src/operator_core/__init__.py
  </files>
  <action>
Create the operator-core package structure:

1. Create `packages/operator-core/pyproject.toml` with:
   - name: "operator-core"
   - version: "0.1.0"
   - requires-python: ">=3.11"
   - dependencies:
     - typer>=0.21.0
     - rich>=14.0.0
     - python-on-whales>=0.70.0
     - httpx>=0.27.0
     - pydantic>=2.0.0
   - Build system: hatchling
   - tool.hatch.build.targets.wheel.packages = ["src/operator_core"]

2. Create `packages/operator-core/src/operator_core/__init__.py` with:
   - __version__ = "0.1.0"
   - Module docstring explaining this is the core operator library

Reference the package pyproject.toml pattern from RESEARCH.md.
  </action>
  <verify>
Run `uv sync` from project root - should install all dependencies.
Run `uv run python -c "import operator_core; print(operator_core.__version__)"` - should print "0.1.0"
  </verify>
  <done>
- operator-core package exists with proper structure
- All dependencies (typer, rich, python-on-whales, httpx, pydantic) installed
- Package is importable
  </done>
</task>

</tasks>

<verification>
1. `uv sync` completes without errors
2. `uv run python -c "import operator_core"` succeeds
3. `uv run python -c "import typer; import rich; import python_on_whales; import httpx; import pydantic"` all succeed
4. Project structure matches:
   ```
   operator/
   ├── pyproject.toml
   ├── uv.lock
   └── packages/
       └── operator-core/
           ├── pyproject.toml
           └── src/
               └── operator_core/
                   └── __init__.py
   ```
</verification>

<success_criteria>
- uv workspace is functional
- operator-core package imports successfully
- All 5 core dependencies are available
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
