[project]
name = "operator-protocols"
version = "0.1.0"
description = "Protocol definitions for the AI-powered operator system"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/operator_protocols"]
