name: tikv-cascading-failures
subjects: [tikv]

# Control plane disruption and node failures that can cascade
chaos_types:
  - type: pd_leader_kill
  - type: node_kill

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
