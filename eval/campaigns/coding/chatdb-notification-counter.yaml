name: chatdb-notification-counter
subjects: [chat-db-app]

# Targets: SELECT COUNT(*) in middleware on every /api/ request
# 500K unread notifications → each request pays 200ms COUNT penalty
chaos_types:
  - type: notification_counter

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
