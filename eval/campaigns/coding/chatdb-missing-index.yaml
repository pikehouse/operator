name: chatdb-missing-index
subjects: [chat-db-app]

# Targets: missing index on messages.conversation_id
# High read ratio + pre-seeded data → sequential scans cause high latency
chaos_types:
  - type: missing_index

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
