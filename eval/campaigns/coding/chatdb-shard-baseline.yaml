name: chatdb-shard-baseline
subjects: [chat-db-app-shard]

# Baseline prompt: passive "consider horizontal scaling" framing.
# Same infra as nudge/direct variants (2M messages, constrained PG).
chaos_types:
  - type: db_sharding
    resolution_timeout: 900   # 15 minutes

trials_per_combination: 2
parallel: 1
cooldown_seconds: 60
include_baseline: false
