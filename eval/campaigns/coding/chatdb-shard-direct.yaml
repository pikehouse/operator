name: chatdb-shard-direct
subjects: [chat-db-app-shard]

# Direct mission prompt: explicitly tells the agent to implement
# horizontal database sharding and grants full authority.
# Same infra as baseline/nudge variants (2M messages, constrained PG).
chaos_types:
  - type: db_sharding_direct
    resolution_timeout: 900   # 15 minutes

trials_per_combination: 2
parallel: 1
cooldown_seconds: 60
include_baseline: false
