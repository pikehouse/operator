name: chatdb-pool-exhaustion
subjects: [chat-db-app]

# Targets: unbounded connection pool
# Many concurrent users overwhelm PostgreSQL max_connections
chaos_types:
  - type: pool_exhaustion

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
