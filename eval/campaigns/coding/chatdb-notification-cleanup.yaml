name: chatdb-notification-cleanup
subjects: [chat-db-app]

# Targets: Unbounded notification accumulation + dead tuple buildup
# Ongoing broadcasts + mark-read creates dead tuples faster than autovacuum
chaos_types:
  - type: notification_cleanup

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
