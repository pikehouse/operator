name: chatdb-notification-mark-read
subjects: [chat-db-app]

# Targets: Row-by-row UPDATE in mark_all_read()
# 50K+ unread per user → each mark-read fires 50K individual UPDATEs
chaos_types:
  - type: notification_mark_read

trials_per_combination: 3
parallel: 1
cooldown_seconds: 10
include_baseline: true
