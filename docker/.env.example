# Rate Limiter Docker Compose Environment Configuration
#
# Copy this file to .env to customize port mappings:
#   cp .env.example .env
#
# Default values are shown below. Change ports if they conflict
# with services already running on your machine.

# Rate Limiter Service Ports
# Each rate limiter node exposes its health and API on a separate host port
RATELIMITER_1_PORT=8001
RATELIMITER_2_PORT=8002
RATELIMITER_3_PORT=8003

# Prometheus Port
PROMETHEUS_PORT=9090

# Load Generator Configuration
LOADGEN_RPS=10
LOADGEN_DURATION=0
LOADGEN_BURST_ENABLED=true
LOADGEN_BURST_MULTIPLIER=5
LOADGEN_BURST_DURATION=5
LOADGEN_BURST_INTERVAL=30
